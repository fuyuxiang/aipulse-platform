# AIPulse Frontend

React 18 + TypeScript + Ant Design Pro + Formily + X6 local control plane.

```bash
cd frontend
npm install
npm run dev
```

The dev server proxies `/api` and WebSocket traffic to `http://127.0.0.1:8000`.

